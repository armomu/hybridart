# 2026-04-23 工作日志

## 移除 Mock 蓝牙模式
- `_sendCommand` 已实现真实 BLE 写入：`BluetoothCharacteristic? _writeChar` 持有写入特征，`_discoverServices` 时赋值，断开时清空，写入用 `char.write(cmd, withoutResponse: false)`
- charger_page.dart 去掉 AppBar 模拟开关和 `_MockDeviceArea` 类，页面固定为真实 BLE 扫描流程
- `connectDevice()` 参数改为非空 `BluetoothDevice`（非 `BluetoothDevice?`）
- version_info_tab.dart 的 MAC 兜底文案从 `'Mock'` 改为 `'—'`

## 修复：充电桩列表连接状态 Bug
- 问题：点击任意设备"连接"时，列表中所有设备的按钮都显示 loading 转圈
- 原因：`_DeviceCard` 仅用全局 `isConnecting.value` 判断，未区分是否为当前卡片
- 修复：
  1. `ChargerController` 新增 `connectingDeviceId = Rxn<String>()` 字段，连接开始时记录目标设备 remoteId，结束后清空
  2. `_DeviceCard` 改用 `isThisDeviceConnecting = ctrl.isConnecting.value && ctrl.connectingDeviceId.value == result.device.remoteId.str` 判断，只有目标设备卡片显示 loading
